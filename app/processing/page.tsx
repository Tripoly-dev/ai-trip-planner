// Screen 4 — Processing. Built out in Step 8 of the build order.
export default function ProcessingScreen() {
  return (
    <main className="flex flex-1 items-center justify-center">
      <p className="text-tripoly-text-muted font-sans text-sm">Processing screen — coming in Step 8.</p>
    </main>
  );
}
