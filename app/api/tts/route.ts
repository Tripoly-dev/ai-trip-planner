// Sarvam AI text-to-speech — built out in Step 6 of the build order.
// See TRIPOLY_HANDOFF.md section 10.
import { NextResponse } from "next/server";

export async function POST() {
  return NextResponse.json({ error: "Not implemented yet — Step 6" }, { status: 501 });
}
